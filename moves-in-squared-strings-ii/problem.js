/*
 * Moves in squared strings (II)
 *
 * You are given a string of n lines, each substring being n characters long.
 *
 * - rot(s): Clock rotation 180 degrees.
 * - selfieAndRot(s): initial string combined with its 180-degree rotation,
 *   interspersed with dots proportional to the length of the segments.
 *
 * Write a high-order function oper(fct, s) where fct is one of rot, selfieAndRot.
 */

function rot(s) {
}

function selfieAndRot(s) {
}

function oper(fct, s) {
}