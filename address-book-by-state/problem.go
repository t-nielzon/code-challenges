/*
Given a string with friends to visit in different states, produce a result
that sorts the names by state and lists the state name followed by each
person residing in that state (people's names sorted).

Each input line has the format: "Name, Street, City ST"

Output format (separator is \n):
- First state: "<StateFullName>"
- Subsequent states: " <StateFullName>" (leading space)
- Each person line: "..... <Name> <Street> <City> <StateFullName>"

States: AZ Arizona, CA California, ID Idaho, IN Indiana, MA Massachusetts,
OK Oklahoma, PA Pennsylvania, VA Virginia.

There can be a blank last line in the input.
*/

package kata

func AddressBook(s string) string {
}