/*
 * Ecco the Dolphin: Ecco Junior
 *
 * Guide Ecco through a 2D underwater maze to reach Tara.
 * Maze characters: ~ (sea), . (air), # (rock), e (Ecco), t (Tara),
 * S (shark), Y (jellyfish), O (urchin)
 *
 * Rules:
 * - Ecco moves in 4 cardinal directions through sea (~) tiles only.
 * - Sharks move horizontally, bouncing off rocks — their entire horizontal lane is dangerous.
 * - Jellyfish move vertically, bouncing off rocks and air — their vertical lane is dangerous.
 * - Urchins expand to all 8 adjacent tiles (including diagonals).
 * - Return array of [y, x] tuples for the path, or null if no path exists.
 */
function solve(maze) {

}

module.exports = { solve };