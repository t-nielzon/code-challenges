/*
 * Multisize Nonogram Encoder
 * 
 * Convert a solved nonogram (represented as a 2D array of 0s and 1s)
 * into clues for rows and columns.
 * 
 * A clue is an array of consecutive 1s in a row or column.
 * For example, [0, 0, 1, 1, 1, 0, 1, 1, 1, 1] produces clues [3, 4].
 * 
 * Input: nonogram - 2D array where 5 <= size <= 100
 * Output: [columnClues, rowClues] where each clue is an array of group lengths
 * 
 * Empty rows/columns (all 0s) should be represented as empty arrays.
 */

function encodeNonogram(nonogram) {
  
}