/*
 * First non-repeating character (5 kyu)
 *
 * Write a function that takes a string input, and returns the first character
 * that is not repeated anywhere in the string.
 *
 * For example, if given the input "stress", the function should return 't',
 * since the letter t only occurs once in the string, and occurs first in the string.
 *
 * As an added challenge, upper- and lowercase characters are considered the
 * same character, but the function should return the correct case for the
 * initial character. For example, the input "sTreSS" should return "T".
 *
 * If a string contains only repeating characters, return an empty string ("").
 *
 * Note: your function should handle any Unicode codepoint.
 */

function firstNonRepeatingLetter(s) {
}

module.exports = firstNonRepeatingLetter;