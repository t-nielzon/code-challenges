/*
 * Largest Palindromic Product
 * 
 * A palindromic number is a number whose digits are in the same order when read both forward and backward.
 * For example, 11, 33, 1331, and 98389 are all palindromic numbers.
 * 
 * Write a function which, when given two integers `lower` and `upper`, returns the largest palindromic
 * number that's a product of any two integers between the inclusive range of `lower` and `upper`.
 * 
 * Examples:
 * - largestPalindromicProduct(10, 99) === 9009 (99 * 91 = 9009)
 * - largestPalindromicProduct(1, 11) === 121 (11 * 11 = 121)
 * 
 * Requirements:
 * - You can use the same number twice
 * - The range is inclusive
 * - A single digit always counts as a palindrome
 * - Return NaN if no palindromic products can be made
 * 
 * Note: This kata is about optimization - naïve solutions will time out.
 */

function largestPalindromicProduct(lower, upper) {
  // Solution here
}