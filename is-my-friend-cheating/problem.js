/*
 * Is my friend cheating?
 *
 * A friend takes the sequence of all numbers from 1 to n (where n > 0).
 * Within that sequence, he chooses two numbers, a and b.
 * The product of a and b should be equal to the sum of all numbers
 * in the sequence, excluding a and b.
 *
 * Given a number n, return all pairs [a, b] where a * b = sum(1..n) - a - b,
 * sorted by increasing a.
 *
 * removNb(26) => [[15, 21], [21, 15]]
 */

function removNb(n) {
}

module.exports = { removNb };