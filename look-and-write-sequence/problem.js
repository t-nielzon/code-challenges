/*
 * Look and Write Sequence
 *
 * You are tasked with a variant of the "look-and-write" sequence. The sequence
 * is built iteratively according to the following rules. Start with the sequence
 * "11". After looking at the current sequence, for each digit 0–9, check how
 * many times it appears. For every digit that occurs at least once, append its
 * count followed by the digit to the end of the sequence.
 *
 * Your task is to compute the length of the sequence after n looks.
 *
 * Input: n, the number of times you look at the sequence (0 ≤ n ≤ 7*10^5)
 * Output: An integer representing the length of the sequence after n looks
 *
 * Examples:
 *   n = 1 → "11" + "21" = "1121" → length 4
 *   n = 3 → "11213112512213" → length 14
 */
function lookAndWrite(n) {

}