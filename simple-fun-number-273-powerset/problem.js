/**
 * Simple Fun #273: Powerset
 * 
 * For the given set S its powerset is the set of all possible subsets of S.
 * 
 * Given an array of integers nums, your task is to return the powerset of its elements.
 * 
 * Implement an algorithm that does it in a depth-first search fashion. That is, for every 
 * integer in the set, we can either choose to take or not take it. At first, we choose NOT 
 * to take it, then we choose to take it.
 * 
 * Example:
 * For nums = [1, 2], the output should be [[], [2], [1], [1, 2]].
 * 
 * For nums = [1, 2, 3], the output should be [[], [3], [2], [2, 3], [1], [1, 3], [1, 2], [1, 2, 3]].
 * 
 * @param {number[]} nums - Array of positive integers, 1 ≤ nums.length ≤ 10
 * @return {number[][]} - The powerset of nums
 */
function powerset(nums) {
}