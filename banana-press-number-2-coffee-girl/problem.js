/*
 * Banana Press #2: Coffee Girl (5 kyu)
 *
 * Given a `tray` (a 2D array of integers) and a `deliverList` (an array of
 * distinct integers), where the integers in the tray match those in the
 * deliverList but are not sorted the same way, return the minimum number of
 * swaps required to order the tray according to the deliverList.
 *
 * Rotations and mirroring of the whole tray are allowed before any swapping.
 * Coffees may be picked from left-to-right or right-to-left (per tray).
 */

function minCoffeeSwaps(tray, deliverList) {
    // your code here
}