/*
Given a tray (a 2d array of integers) and a deliverList (an array of distinct integers), 
in which the integers in the tray match the integers in the deliverList but not sorted 
in the same way, create a function minCoffeeSwaps(tray, deliverList) which returns the 
minimum number of swaps required to order the tray according to the deliverList. 

Rotations (0, 90, 180, 270 degrees) and reading directions (left-to-right or right-to-left) 
of the whole tray are allowed before any swapping.

Example 1:
const tray = [
    [1, 2, 7],
    [4, 5, 6],
    [3, 8, 9]
];
const deliverList = [1,2,3,4,5,6,7,8,9];
minCoffeeSwaps(tray, deliverList) // 1

Example 2:
const tray = [
    [1, 4, 7],
    [2, 5, 8],
    [3, 6, 9]
];
const deliverList = [1,2,3,4,5,6,7,8,9]
minCoffeeSwaps(tray, deliverList) // 0
*/

function minCoffeeSwaps(tray, deliverList) {
  // your code here
}