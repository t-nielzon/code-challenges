/*
 * Playing with laziness
 * https://www.codewars.com/kata/playing-with-laziness
 * Difficulty: 5 kyu
 *
 * Given a 2D infinite matrix generated via ES6 proxies where every element
 * is false except one at indices (y, x), write a function that finds and
 * returns the [y, x] pair where the matrix is true.
 *
 * The matrix is created by the preloaded `generate(y, x)` function using
 * Proxy objects. You can access elements with mat[i][j], but array methods
 * do not work. The matrix is conceptually infinite in both dimensions.
 */

function findTrue(matrix) {
}