/*
 * Single character palindromes
 *
 * You will be given a string and you task is to check if it is possible to
 * convert that string into a palindrome by removing a single character.
 *
 * If the string is already a palindrome, return "OK".
 * If it is not, and we can convert it to a palindrome by removing one
 * character, then return "remove one".
 * Otherwise return "not possible".
 *
 * The order of the characters should not be changed.
 *
 * For example:
 *   "abba"   -> "OK"           - this is a palindrome
 *   "abbaa"  -> "remove one"   - remove the 'a' at the extreme right
 *   "abbaab" -> "not possible" - cannot be made a palindrome
 */

function solve(s) {
  // your code here
}