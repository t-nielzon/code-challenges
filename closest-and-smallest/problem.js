/*
 * Closest and Smallest
 *
 * Input:
 *   a string `strng` of n positive numbers (n = 0 or n >= 2)
 *
 * Weight of a number = sum of its digits.
 *
 * Task:
 *   For each number in `strng` calculate its weight and find two numbers that have:
 *     - the smallest difference of weights (closest)
 *     - with the smallest weights
 *     - and with the smallest indices in `strng`
 *
 * Output:
 *   An array of two arrays, each in the format:
 *     [number-weight, index in strng, original corresponding number in strng]
 *
 *   The two subarrays are sorted ascending by their weights, or by index if weights are equal.
 *
 *   If n == 0, return [].
 */

function closest(strng) {
  // your code here
}