/*
 * Amidakuji
 *
 * Amidakuji is a method of lottery designed to create random pairings
 * between two sets comprised of an equal number of elements.
 *
 * Write a function amidakuji that returns the final positions of each element.
 * Elements are an ascending sequence of consecutive integers starting with 0
 * (from left to right).
 *
 * Input: an array of equal-length strings of '0' and '1' characters representing
 * a ladder. '1's are rungs, '0's are empty space. Each element starts at the top
 * of its corresponding vertical rail. During descent, when a rail intersects a
 * rung, it swaps values with the adjacent connecting rail.
 *
 * Output: an array of integers with each integer in its final position.
 *
 * No two rungs will ever be adjacent.
 * Maximum width: 20, maximum height: 50.
 */
function amidakuji(ladder) {
}