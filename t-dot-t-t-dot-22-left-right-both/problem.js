/**
 * T.T.T.22: Left? Right? Both?
 * 
 * Difficulty: 6 kyu
 * 
 * You have a keyboard with a left side and right side:
 *   L     1.....5 | 6.....0     R
 *   E     Q.....T | Y.....P     I
 *   F     A.....G | H.....'     G
 *   T     Z.....B | N...../     H
 *            SPACEBAR           T
 * 
 * Complete the function that accepts a string and determines:
 * - if the string can be typed by the left hand only, return "Left"
 * - if the string can be typed by the right hand only, return "Right"
 * - if both hands are needed to type the string, return "Both"
 * - if the string is empty or contains only spaces, return an empty string ""
 * 
 * The spacebar is an exception and can be typed by either hand.
 * 
 * @param {string} str - The string to type
 * @returns {string} "Left", "Right", "Both", or ""
 */
function handType(str) {
  
}