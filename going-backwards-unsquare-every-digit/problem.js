/**
 * Going backwards: Unsquare every digit
 * 
 * Given the result of squaring every digit of a number and concatenating them,
 * return the original number.
 * 
 * For example:
 * - If the input is 811181, the original number is 9119
 *   (because 9² = 81, 1² = 1, 1² = 1, 9² = 81 → "811181")
 * - If the input is 493625, the original number is 765
 *   (because 7² = 49, 6² = 36, 5² = 25 → "493625")
 * 
 * If there is more than one possibility, return the smallest.
 * If there are no possibilities, return null.
 * 
 * @param {number} num - A non-negative integer
 * @returns {number|null} - The original number or null if no valid number exists
 */
function unsquareDigit(num) {
  // Your code here
}