/*
 * Mystery function #1
 * Difficulty: 5 kyu
 *
 * There's a mystery function called `mystery` which accepts a string
 * and outputs a string. The exercise is to guess what it does by
 * calling it with different inputs and analyzing the outputs, then
 * reimplement it in a function called `solved(x)` that returns the
 * same output as `mystery` for the same inputs.
 *
 * Example:
 *   let myOutput = mystery('myInput');
 *
 * Your `solved(x)` must match `mystery(x)` for all inputs.
 */

function solved(x) {

}