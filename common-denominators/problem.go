/*
Common Denominators

You will have a list of rationals in the form of pairs [numerator, denominator]
where all numbers are positive integers.

You have to produce a result where D is the least common multiple of all
denominators, and for each fraction N_i/D == numer_i/denom_i.

Example:
convertFracs [(1, 2), (1, 3), (1, 4)] should return [(6, 12), (4, 12), (3, 12)]

Note: Input fractions should be reduced first to minimize the common denominator.
*/

func ConvertFracs(fracs [][2]int) [][2]int {
	// TODO: implement
}