/*
 * Tribonacci Sequence
 *
 * It works basically like a Fibonacci, but summing the last 3 (instead of 2)
 * numbers of the sequence to generate the next.
 *
 * Starting with the signature [1, 1, 1]:
 *   [1, 1, 1, 3, 5, 9, 17, 31, ...]
 *
 * Starting with the signature [0, 0, 1]:
 *   [0, 0, 1, 1, 2, 4, 7, 13, 24, ...]
 *
 * Given a signature array (always 3 numbers), return the first n elements -
 * signature included - of the so seeded sequence.
 *
 * n will always be a non-negative number; if n == 0, return an empty array.
 */

function tribonacci(signature, n) {
  // your code here
}