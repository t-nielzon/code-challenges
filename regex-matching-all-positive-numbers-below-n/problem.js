// Regular expression pattern to match positive numbers strictly less than n
// The pattern should only match decimal representations of positive integers k
// where 0 < k < n, without leading zeros

function patternNumbers(n) {
    // Return regex pattern as string
}