// Regex matching all positive numbers below n
// 
// Given a positive natural number n, create a regular expression pattern
// which only matches decimal representations of all positive natural numbers
// strictly less than n, without leading zeros.
// 
// The regex should not match:
// - empty string
// - numbers with leading zeros
// - negative numbers
// - non-numbers
// 
// Should only match decimal representations of numbers k where 0 < k < n

function regex(n) {
  
}