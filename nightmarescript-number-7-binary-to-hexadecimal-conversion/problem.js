/*
 * NightmareScript#7 — Binary to Hexadecimal Conversion
 *
 * NightmareScript is JavaScript without alpha-numeric characters.
 *
 * The only permitted characters are:
 *   ! @ # $ % ^ & * ( ) _ + - | < > . , ' ; : " ` ~ = ? /\ { } [ ]
 * Spaces and new lines are also acceptable.
 *
 * Task:
 *   Convert a Binary string to a Hexadecimal string.
 *
 * Rules:
 *   - Character limit of 500 to prevent copy pasting from tools like JSFuck.
 *   - Golfing encouraged but not required.
 *   - Binary strings will always have a length divisible by 4.
 *   - Your Hexadecimal string should use lowercase letters, e.g. "ff0f".
 *   - You will have to convert 999 random 16384 bit strings in random tests,
 *     so efficiency matters.
 */

function binToHex(binary) {
  // your code here
}