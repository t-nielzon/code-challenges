/*
 * Conway's Game of Life is a 2D cellular automaton.
 * Given a non-negative integer N, return a life that fully dies out after N steps.
 * Represent it as a list of 2D points [x, y].
 * 
 * Rules:
 * - A cell with 2-3 neighbors stays alive
 * - A cell with <2 or >3 neighbors dies
 * - A dead cell with exactly 3 neighbors becomes alive
 * - After N steps, all cells must be dead with no remaining patterns
 */

function omnifinite(n) {
  
}