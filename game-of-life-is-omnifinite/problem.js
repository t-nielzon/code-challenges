/*
 * Game of Life Omnifinite
 * 
 * Conway's Game of Life is a 2D cellular automaton.
 * A life may die out after any number of steps, making Game of Life omnifinite.
 * 
 * Given a non-negative integer N, return a life that fully dies out after N steps,
 * assuming an infinite grid. Represent it as a list of 2D points [x, y].
 * 
 * Rules:
 * - Any live cell with 2 or 3 live neighbors survives
 * - Any dead cell with exactly 3 live neighbors becomes alive
 * - All other cells die or stay dead
 */

function gameOfLifeOmnifinite(n) {
  // TODO: Return array of [x, y] coordinates that die after exactly n steps
}