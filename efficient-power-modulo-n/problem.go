// Modpow computes (x^y)%n efficiently for large y
// using binary exponentiation (exponentiation by squaring).
// This avoids the overflow issues of computing x^y directly
// and provides O(log y) time complexity.
func Modpow(x int64, y int64, n int64) int64 {
	
}