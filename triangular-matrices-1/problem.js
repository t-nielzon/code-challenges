/*
 * Triangular Matrices (6 kyu)
 *
 * A square matrix is considered upper-triangular if all the entries below
 * the main diagonal are 0. Similarly, a square matrix is considered
 * lower-triangular if all the entries above the main diagonal are 0.
 *
 * Given a square matrix of size n > 1, determine if it is upper triangular
 * or lower triangular. The matrix (mat) is represented by a nested array of rows.
 *
 * Examples:
 *   upperTriangular([[1,1,1],[0,1,1],[0,0,1]]) // => true
 *   upperTriangular([[0,0,0],[0,0,0],[1,0,0]]) // => false
 *   lowerTriangular([[1,0],[1,1]]) // => true
 *   lowerTriangular([[0,1],[0,0]]) // => false
 */

function upperTriangular(mat) {
}

function lowerTriangular(mat) {
}