package kata

/*
We search non-negative integer numbers, with at most 3 digits, such as the sum
of the cubes of their digits is the number itself; we will call them "cubic" numbers.

153 is such a "cubic" number : 1^3 + 5^3 + 3^3 = 153

These "cubic" numbers of at most 3 digits are hidden with other numbers and
characters in a string. The task is to find the "cubic" numbers in the string
and return their sum.

Return format: "number1 number2 (and so on) sumOfCubicNumbers Lucky"
If no cubic numbers found: "Unlucky"

The numbers in the output are in the order they appear in the input string.
When a continuous string of digits exceeds 3, split into groups of 3 from the left.
*/

func SumOfCubic(s string) string {
	panic("implement me")
}