/*
 * Street Fighter 2 - Character Selection - Part 2
 *
 * Given a rectangular grid of fighters (with possible empty string spaces),
 * an initial position, and a list of moves, return the list of fighters
 * hovered after each move.
 *
 * Rules:
 * - Horizontal movement wraps around (rotates).
 * - Vertical movement stops at grid edges.
 * - Empty spaces ("") are skipped when approached horizontally
 *   (continue in that direction until a non-empty slot is found).
 * - Empty spaces block vertical movement (stay at current position).
 * - The grid can be any rectangular size with empty spaces anywhere.
 */
function superStreetFighterIITurbo(fighters, position, moves) {
}