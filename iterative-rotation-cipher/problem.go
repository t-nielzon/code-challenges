package main

/*
 * Iterative Rotation Cipher (IRC)
 * 
 * Implement two functions: Encode and Decode
 * 
 * Encode(n, s):
 * - Performs n iterations of:
 *   1. Remove all spaces, track their positions
 *   2. Rotate the entire string right by n characters
 *   3. Put spaces back at their original positions
 *   4. Rotate each space-separated substring right by n characters
 * - Returns the string with n prepended and a space
 * 
 * Decode(s):
 * - Takes an encoded string (with n prepended)
 * - Reverses the encoding process by performing n reverse iterations
 * - Returns the original decoded string
 */

func Encode(n int, s string) string {
	// TODO: implement
	return ""
}

func Decode(s string) string {
	// TODO: implement
	return ""
}