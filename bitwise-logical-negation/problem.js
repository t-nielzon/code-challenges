/*
Bitwise logical negation

Given a number n, return the logical negation of n, i.e., return 1 if n = 0, 
otherwise return 0.

Restrictions:
- Maximum 28 characters
- Only allowed characters: [0-9], [a-z], (, ), +, ~, &, |, ^, <, =, >
- At most 2 uses of +
- Cannot use: &&, ||, <, <=, ==, ===, >=, >
- Can use: +, ~, &, |, ^, <<, >>, >>>

Examples:
  negation(0)  // 1
  negation(1)  // 0
  negation(-1) // 0
  negation(32) // 0
*/

function negation(n) {
}