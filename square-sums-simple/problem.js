/*
Square sums (simple)

Write function squareSumsRow that, given integer number N (in range 2..43),
returns array of integers 1..N arranged in a way, so sum of each 2
consecutive numbers is a square.

Solution is valid if and only if following two criterias are met:
1. Each number in range 1..N is used once and only once.
2. Sum of each 2 consecutive numbers is a perfect square.

If there is no solution, return false.
*/

function squareSumsRow(n) {
}

module.exports = squareSumsRow;