/*
 * In some programming languages, strings internally are implemented like an array of chars.
 *
 * Add to the String.prototype the next Array.prototype methods:
 *  - map, join, filter, forEach, some, every, reduce, reduceRight, sort, reverse
 *
 * Also implement mutable methods that return the resulting string instead of the
 * value the equivalent array method returns:
 *  - push, pop, shift, unshift, splice
 *
 * Example:
 *   "Hello".push(" Peter"); // "Hello Peter"
 *   "Hello".pop();          // "Hell"
 */

// Add the methods to String.prototype here.