/*
 * Max wants to improve his chess skills. To achieve his goal he decided to do
 * exercises. Help Max at his (K)nightmares by solving this Kata.
 *
 * Task
 * Return the minimum N (amount of moves) needed on an 8x8 board starting with
 * a white pawn from the initial position `start` ("a2" to "h7", 1st and 8th
 * rank excluded) to target position `target` ("a1" to "h8"). If `start` and
 * `target` are equal then return 0.
 *
 * Notes
 * - There are no pieces on the board except for the pawn on `start`.
 * - A pawn can only move one square up.
 * - A pawn on the 2nd rank can NOT move 2 squares at once.
 * - If the pawn reaches the 8th rank it will be promoted to a Queen or Knight.
 *   It is not required to promote if the pawn can reach the target only by
 *   moving forward.
 * - The Queen moves in any direction with no limits, but cannot imitate a
 *   Knight's move.
 */

function chessPawn(start, target) {

}