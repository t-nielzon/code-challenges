// Ludic numbers
// The ludic numbers are generated using a sieving method similar to prime generation.
// Starting with 1 and natural numbers 2,3,4,...
// Repeatedly: take the first element as the sieve value, add it to ludic numbers,
// remove it, then remove every sieve-th element from the remaining list.

function sumLudic(n) {
  // Return the sum of the first n ludic numbers
}