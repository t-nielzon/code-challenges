/*
 * Basic markdown to HTML converter.
 * Transform markdown elements to HTML: headers (1-6 levels), list items, and bold emphasis.
 * Input is a single line of markdown with no newlines.
 */

function format(text) {
  
}