/*
 * Color Choice
 *
 * Knowing m (number of posters to design) and n (total number of available colors),
 * find x, the number of colors for each poster so that each poster has a unique
 * combination of colors and the number of combinations is exactly m.
 *
 * In other words, find x such that:
 *   n choose x = m
 *
 * for a given m >= 0 and a given n > 0.
 * If there are several solutions, return the smallest. If there are no solutions, return -1.
 *
 * Examples:
 *   (m =  6, n = 4) -->  2
 *   (m =  4, n = 4) -->  1
 *   (m =  4, n = 2) --> -1
 *   (m = 35, n = 7) -->  3
 *   (m = 36, n = 7) --> -1
 */

function chooseColour(m, n) {

}