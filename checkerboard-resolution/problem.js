/*
 * Checkerboard Resolution
 * 
 * Count the number of individual black squares on a special checkerboard.
 * The board has a given width and height, and a resolution that determines
 * the size of each color square. The top-left square is always white.
 * 
 * The checkerboard alternates colors in a standard pattern where positions
 * with an even index sum are white and odd sums are black.
 * 
 * Examples:
 * - countBlack(11, 6, 1) = 33
 * - countBlack(11, 6, 2) = 32
 * - countBlack(11, 6, 5) = 31
 */

function countBlack(width, height, resolution) {
  
}