/*
 * Morse Encoding (5 kyu)
 *
 * You are writing an encoder/decoder to convert between javascript strings
 * and a binary representation of Morse code.
 *
 * Each Morse code character is represented by a series of "dots" and "dashes".
 * In binary, a dot is a single bit (1) and a dash is three bits (111).
 * Between each dot or dash within a single character, we place a single zero bit.
 * Separate characters are separated by three zero bits (000).
 * Words are separated by a single space, represented by 7 zero bits (0000000).
 *
 * Morse.encode(message) - takes a string, returns an array of signed 32-bit integers
 * Morse.decode(arr) - takes an array of 32-bit integers, returns a string
 */

var Morse = {
  encode: function(message) {
  },
  decode: function(data) {
  }
};