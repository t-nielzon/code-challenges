/*
 * There are 8 balls numbered from 0 to 7.
 * Seven of them have the same weight. One is heavier. Find its number.
 *
 * findBall(scales) receives a scales object with:
 *   - an internal array of 8 elements (one is heavier)
 *   - getWeight(left, right): returns -1 (left heavier), 1 (right heavier), 0 (equal)
 *
 * You may call getWeight at most 3 times.
 */
function findBall(scales) {
  // your code here
}