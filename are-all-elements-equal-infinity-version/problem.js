/*
  Are all elements equal? (Infinity version)
  
  Create a function `eqAll` that determines if all elements of a list are equal.
  The list can be any iterable, and may be infinite.
  
  For the function result to be `true`, the iterable must be finite.
  However, `false` can result from an element finitely far from the left end.
  Elements will be primitive values; equality should be strict (===).
  
  Examples:
  eqAll("aaa") => true
  eqAll("abc") => false
  eqAll("")    => true
  eqAll([0,0,0]) => true
  eqAll([0,1,2]) => false
  eqAll([])      => true
*/

function eqAll(list) {
  // Your solution here
}