/*
 * Are they the "same"?
 * 
 * Given two arrays `a` and `b` write a function `comp(a, b)` that checks 
 * whether the two arrays have the "same" elements, with the same multiplicities 
 * (the multiplicity of a member is the number of times it appears). 
 * "Same" means that the elements in `b` are the elements in `a` squared, 
 * regardless of the order.
 * 
 * Examples:
 * a = [121, 144, 19, 161, 19, 144, 19, 11]
 * b = [121, 14641, 20736, 361, 25921, 361, 20736, 361]
 * comp(a, b) returns true
 * 
 * If a or b are null/undefined, return false.
 * If a or b are not arrays, return false.
 */

function comp(a, b){
  
}