/*
 * Enough is enough!
 * 
 * Alice and Bob were on a holiday. Both of them took many pictures of the places they've been, 
 * and now they want to show Charlie their entire collection. However, Charlie doesn't like 
 * these sessions, since the motif usually repeats. He isn't fond of seeing the Eiffel tower 40 times.
 * He tells them that he will only sit for the session if they show the same motif at most N times. 
 * Luckily, Alice and Bob are able to encode the motif as a number.
 * 
 * Given a list and a number, create a new list that contains each number of list at most N times, 
 * without reordering.
 * 
 * Example 1: Input [1,2,3,1,2,1,2,3] with N=2
 *           Output [1,2,3,1,2,3]
 * 
 * Example 2: Input [20,37,20,21] with N=1
 *           Output [20,37,21]
 */

function deleteNth(arr, n) {
  // Your solution here
}