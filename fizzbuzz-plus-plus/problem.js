// FizzBuzz++
// Given a list of coprime numbers and a list of words, compute the fizzbuzz representation
// up until the pattern of strings repeats itself.
// 
// For each number from 1 onwards:
// - If divisible by any of the coprime numbers, concatenate the corresponding words
// - Otherwise, include the number itself
// - Continue until the pattern repeats (at the LCM of all numbers)
//
// Returns an array of numbers and strings

function fizzbuzzPlusPlus(numbers, words) {
  // body
}