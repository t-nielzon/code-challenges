/**
 * Simple Fun #56: Beautiful Text
 * 
 * Consider a string containing only letters and whitespaces. It is allowed to replace some
 * (possibly, none) whitespaces with newline symbols to obtain a multiline text.
 * 
 * A multiline text is beautiful if each of its lines contains an equal number of characters
 * (only letters and whitespaces count; newlines don't).
 * 
 * Given a string s and integers l and r (l ≤ r), check if it's possible to obtain a
 * beautiful text from the string with a text width (line length) in the range [l, r].
 * 
 * Example:
 * For s = "Look at this example of a correct text", l = 5, r = 15
 * Output: true (can format as width 12:
 *   Look at this
 *   example of a
 *   correct text
 * )
 */

function isBeautifulText(s, l, r) {
  
}