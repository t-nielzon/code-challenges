/**
 * Impossible Palindrome Regex
 * Difficulty: 5 kyu
 * 
 * Define a constant `palindromeMatcher`, which will match palindromes of any 
 * (nonzero) length when tested with String.prototype methods:
 * - .match
 * - .matchAll
 * - .replace
 * - .search
 * - .split
 * 
 * A palindrome is a single 'word' of length >= 1, which remains the same when 
 * reversed (case-insensitive). A 'word' is a sequence of letters (a-z, A-Z) 
 * which is not preceded or succeeded by another letter.
 * 
 * Matches should be global and should not contain any capture groups.
 */

const palindromeMatcher = undefined;