/*
 * Simple reversed parenthesis
 *
 * Given a string, return the minimum number of parenthesis reversals
 * needed to make the parentheses balanced.
 *
 * For example:
 *   solve(")(")     = 2  // Reverse ")" to "(" and "(" to ")". 2 reversals.
 *   solve("(((())") = 1  // Reverse one "(" to ")" to make it balanced.
 *   solve("(((")    = -1 // Not possible to balance, so return -1.
 *
 * Each character will be either "(" or ")".
 */

function solve(s) {

}