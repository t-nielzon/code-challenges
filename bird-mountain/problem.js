/*
 * Bird Mountain - 5 kyu
 *
 * A bird flying high above a mountain range is able to estimate
 * the height of the highest peak.
 *
 * Given a birds-eye view of a mountain range (an array of strings
 * where '^' marks mountain cells and spaces mark non-mountain),
 * return the height of the highest peak.
 *
 * The height is determined by peeling layers: every '^' that touches
 * (4-directionally) a non-'^' cell or the edge belongs to layer 1.
 * Then every remaining '^' adjacent to a layer-1 cell belongs to layer 2,
 * and so on. The answer is the highest layer reached.
 */

function peakHeight(mountain) {
  // your code here
}