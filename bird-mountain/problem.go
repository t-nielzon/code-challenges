/*
A bird flying high above a mountain range is able to estimate the height of the
highest peak.

Can you?

The bird looks down on the mountain range from above. The peaks are drawn as a
grid of '^' characters (with spaces around them). The bird repeatedly "peels"
away the outer layer of the mountain: every '^' that touches a space (or the
edge of the grid) is one unit high. Those are marked and removed, then the next
layer of '^' characters now touching the removed cells form the next height,
and so on.

The height of the highest peak is the number of layers needed to consume the
whole mountain.

Given the birds-eye view as a slice of strings, return the height of the
highest peak.

Example:

	^^^^^^
	 ^^^^^^^^
	  ^^^^^^^
	  ^^^^^
	  ^^^^^^^^^^^
	  ^^^^^^
	  ^^^^

Height = 3
*/

package kata

func PeakHeight(mountain []string) int {
	// your code here
	return 0
}