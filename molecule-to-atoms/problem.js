/*
 * For a given chemical formula represented by a string, count the number of atoms
 * of each element contained in the molecule and return an object.
 *
 * Examples:
 * parseMolecule('H2O');           // {H: 2, O: 1}
 * parseMolecule('Mg(OH)2');       // {Mg: 1, O: 2, H: 2}
 * parseMolecule('K4[ON(SO3)2]2'); // {K: 4, O: 14, N: 2, S: 4}
 *
 * Brackets may be round (), square [], or curly {} and can be nested.
 * Index after brackets is optional.
 */

function parseMolecule(formula) {
  
}