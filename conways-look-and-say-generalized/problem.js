/*
 * Conway's Look and Say - Generalized
 * Difficulty: 5 kyu
 *
 * Your task is to create a function that will take an integer and return the
 * result of the look-and-say function on that integer. This should be a general
 * function that takes as input any positive integer, and returns an integer;
 * inputs are not limited to the sequence which starts with "1".
 *
 * Conway's Look-and-say sequence:
 * - Start with a number.
 * - Look at the number, and group consecutive digits together.
 * - For each digit group, say the number of digits, then the digit itself.
 *
 * Examples:
 *   0       -> 10
 *   2014    -> 12101114
 *   9000    -> 1930
 *   22322   -> 221322
 *   222222222222 -> 122
 */

function lookSay(num) {
  // your code here
}