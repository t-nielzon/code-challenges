/*
"Stop it, HTML!"

You don't quite like HTML.
In fact, you like what HTML does but you find annoying to write HTML, if not even expensive!
So you decided to explore new ways to write YOUR template language.

You need to support the following tags:
['html','head','title','body','script','link','div','span','img','br','hr']

In HTML, some tags - link, img, br, hr - can be either self-closed or left open.
Tests require the <br /> version.

Each element function signature: element(attributes, content)
- attributes: object (required, can be empty)
- content: array of strings (optional, can be undefined)

loadMyTemplateLanguage() is a setup function that will be called before any assertions.
You may use it to your advantage to make the tag functions available globally.
*/

function loadMyTemplateLanguage() {
  
}