package main

/*
In number theory, Euler's totient is an arithmetic function that counts the positive
integers less than or equal to n that are relatively prime to n.
Thus, φ(n) is the number of positive integers k ≤ n for which gcd(n, k) = 1.

For example, φ(9) = 6 (the numbers 1, 2, 4, 5, 7, 8 are relatively prime to 9).
Also, φ(36) = 12 and φ(1) = 1.

You can use the Euler product formula:
φ(n) = n * Product(1 - 1/p) where the product is over primes p that divide n.

Input range: 1 ≤ n ≤ 1e10
*/

func Phi(n int64) int64 {
	return 0
}