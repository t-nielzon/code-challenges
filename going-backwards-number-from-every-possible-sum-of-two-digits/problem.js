/*
 * Going backwards: Number from every possible sum of two digits
 * 
 * Given a number >= 0, return all the possible sums of two digits of it.
 * In the output, the pairs of digits should be ordered first left-to-right for the first digit,
 * then left-to-right for subsequent digits.
 * 
 * For example, 12345: all possible sum of two digits from that number are:
 * [ 1 + 2, 1 + 3, 1 + 4, 1 + 5, 2 + 3, 2 + 4, 2 + 5, 3 + 4, 3 + 5, 4 + 5 ]
 * Therefore the result must be:
 * [ 3, 4, 5, 6, 5, 6, 7, 7, 8, 9 ]
 * 
 * We now interrupt your regularly scheduled programming.
 * 
 * Given the result, return the number!
 * (if there is more than one possibility, just return any one of them)
 */

function findNumber(sums) {
  // Your solution here
}