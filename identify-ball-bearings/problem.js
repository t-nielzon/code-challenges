/*
 * Identify Ball Bearings
 *
 * Failure on the factory floor!! One box of 'deluxe' ball-bearings has been
 * mixed in with all the boxes of 'normal' ball-bearings! We need your help to
 * identify the right box!
 *
 * Information:
 * - 'deluxe' ball-bearings weigh exactly 11 grams
 * - 'normal' ball-bearings weigh exactly 10 grams
 * - Besides weight, both kinds of ball-bearings are identical
 * - There are (effectively) infinite bearings in each box
 * - Each box contains exclusively one type of bearing (either normal, or 'deluxe')
 *
 * To help you identify the right box, you also have access to a Super Scale™
 * which will tell you the exact weight of anything you give it. Unfortunately,
 * getting it ready for each measurement takes a long time, so you only have
 * time to use it once!
 *
 * Task:
 * Write a function which accepts two arguments:
 * - `bearings`: A list of the bearing types contained in each 'box'.
 *   (length between 1 and 200 inclusive)
 * - `weigh`: a function which accepts any number of arguments, returning the
 *   total weight of all. Can only be used once!
 *
 * Your function should identify and return the single 'deluxe' bearing sample
 * from `bearings`.
 */

function identifyBB(bearings, weigh) {
  // your code here
}