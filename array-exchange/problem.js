/*
 * Array Exchange and Reversing
 *
 * It's time for some array exchange! The objective is simple: exchange the
 * elements of two arrays in-place in a way that their new content is also
 * reversed. The arrays may be of unequal lengths, in which case you will
 * have to expand the shorter one in-place.
 *
 * Example:
 *   my_array = ['a', 'b', 'c']
 *   other_array = [1, 2, 3]
 *
 *   exchangeArrays(my_array, other_array)
 *
 *   my_array == [3, 2, 1]
 *   other_array == ['c', 'b', 'a']
 */

function exchangeArrays(a, b) {
}