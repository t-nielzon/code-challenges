/**
 * The Supermarket Queue (6 kyu)
 * 
 * There is a queue for the self-checkout tills at the supermarket. 
 * Your task is write a function to calculate the total time required 
 * for all the customers to check out!
 * 
 * @param {number[]} customers - array of positive integers representing the queue.
 *                               Each integer represents a customer, and its value 
 *                               is the amount of time they require to check out.
 * @param {number} n - a positive integer, the number of checkout tills.
 * @returns {number} - the total time required for all customers to check out.
 * 
 * Examples:
 * queueTime([5,3,4], 1) => 12
 * queueTime([10,2,3,3], 2) => 10
 * queueTime([2,3,10], 2) => 12
 */

function queueTime(customers, n) {
  
}