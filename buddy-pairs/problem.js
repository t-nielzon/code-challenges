/*
 * Buddy Pairs
 *
 * The divisors of a positive integer n are said to be proper when you consider
 * only the divisors other than n itself. Let s(n) be the sum of these proper
 * divisors of n. Two positive integers (n, m) are a buddy pair if:
 *   s(m) = n + 1 and s(n) = m + 1
 *
 * Given two positive integers start and limit, return the first pair [n, m]
 * of buddy pairs such that n is between start (inclusive) and limit (inclusive);
 * m can be greater than limit and must be greater than n.
 *
 * If there is no buddy pair satisfying the conditions, return "Nothing".
 */
function buddy(start, limit) {
  // your code here
}