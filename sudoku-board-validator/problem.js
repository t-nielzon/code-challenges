/*
 * Sudoku Board Validator
 *
 * Sudoku is a game played on a 9x9 grid. The goal of the game is to fill all
 * cells of the grid with digits from 1 to 9, so that each column, each row,
 * and each of the nine 3x3 sub-grids (also known as blocks) contain all of
 * the digits from 1 to 9.
 *
 * Write a function that accepts a Sudoku board, and returns true if it is a
 * valid Sudoku solution, or false otherwise. The cells of the input Sudoku
 * board may also contain 0's, which will represent empty cells. Boards
 * containing one or more zeroes are considered to be invalid solutions.
 *
 * - All inputs are guaranteed to be 2D boards of size 9x9 with possible
 *   values in range 0-9.
 * - Rows, columns and blocks (3x3 small squares) must contain each number
 *   from range 1-9 exactly once.
 * - User solution must not modify input boards.
 */

function validSolution(board) {
}