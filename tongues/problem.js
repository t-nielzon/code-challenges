/*
 * Tongues (5 kyu)
 *
 * Gandalf used a simple letter substitution scheme that is its own inverse.
 *
 * Vowels are replaced in the sequence 'a' 'i' 'y' 'e' 'o' 'u' by advancing
 * three positions cyclically, preserving case.
 *
 * Consonants are replaced from the sequence
 * 'b' 'k' 'x' 'z' 'n' 'h' 'd' 'c' 'w' 'g' 'p' 'v' 'j' 'q' 't' 's' 'r' 'l' 'm' 'f'
 * by advancing ten positions.
 *
 * The transformation is its own inverse.
 *
 * Input: a string of up to 100 plain ASCII characters (32-126).
 * Output: the decoded string.
 */

function tongues(code) {
}

module.exports = { tongues };