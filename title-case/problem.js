/*
 * A string is considered to be in title case if each word in the string is either
 * (a) capitalised (only the first letter in upper case) or (b) considered to be an
 * exception and put entirely into lower case unless it is the first word, which is
 * always capitalised.
 *
 * Write a function that converts a string into title case, given an optional list
 * of exceptions (minor words). The list of minor words will be given as a string
 * with each word separated by a space. The function should ignore the case of the
 * minor words string.
 *
 * Example:
 *   titleCase('a clash of KINGS', 'a an the of') // 'A Clash of Kings'
 *   titleCase('THE WIND IN THE WILLOWS', 'The In') // 'The Wind in the Willows'
 *   titleCase('the quick brown fox') // 'The Quick Brown Fox'
 */

function titleCase(title, minorWords) {
}