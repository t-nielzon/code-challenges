/*
 * Pascal's Triangle
 * 
 * In mathematics, Pascal's triangle is a triangular array of the binomial coefficients
 * expressed with formula C(n,k) = n! / (k!(n-k)!), where n denotes a row of the triangle,
 * and k is a position of a term in the row.
 * 
 * Task: Given a depth n, return n top rows of Pascal's Triangle flattened into a 
 * one-dimensional list/array.
 * 
 * Examples:
 * n = 1: [1]
 * n = 2: [1, 1, 1]
 * n = 4: [1, 1, 1, 1, 2, 1, 1, 3, 3, 1]
 */

function pascalsTriangle(n) {
  
}