/*
Easy Balance Checking
Difficulty: 6 kyu

You are given a (small) check book as a - sometimes - cluttered (by non-alphanumeric characters) string.

The first line shows the original balance.
Each other line (when not blank) gives information: check number, category, check amount.

First you have to clean the lines keeping only letters, digits, dots and spaces.

Then return a report as a string with:
- Original Balance
- For each check: check number, category, amount, and running balance
- Total expense
- Average expense

All values rounded to 2 decimals.
*/

package main

func Balance(s string) string {
	// TODO: implement
	return ""
}