package main

/*
Find the Partition with Maximum Product Value

You are given a certain integer, n, n > 0.
You have to search the partition or partitions, of n, with maximum product value.

A partition of n is a way of writing n as a sum of positive integers.
For each partition, calculate the product of its elements.

Returns a tuple of:
- A list of all partitions with maximum product, sorted by increasing length
- The maximum product value

Examples (input -> output):
8 -> ([[3, 3, 2]], 18)
10 -> ([[4, 3, 3], [3, 3, 2, 2]], 36)
*/

func MaxPartitionProduct(n int) ([][]int, int) {
	return nil, 0
}