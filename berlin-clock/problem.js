/*
 * Berlin Clock
 * 
 * The Berlin Clock tells time using illuminated colored fields:
 * - Top light (Y): blinks on for even seconds, off for odd seconds
 * - Row 1 (4 R fields): each represents 5 hours
 * - Row 2 (4 R fields): each represents 1 hour
 * - Row 3 (11 Y/R fields): each represents 5 minutes (R marks 15, 30, 45 min)
 * - Row 4 (4 Y fields): each represents 1 minute
 * 
 * Input: time in 24h format "hh:mm:ss"
 * Output: string representation of the Berlin Clock with rows joined by newlines
 */

function berlinClock(time) {
  // TODO: implement solution
}