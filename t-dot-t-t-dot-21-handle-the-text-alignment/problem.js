/*
 * T.T.T.21: Handle the text alignment
 *
 * You are a teacher and here is a list of test scores.
 * Please output them in accordance with the rules.
 *
 * Complete function formatScoreList() that accept an object array list
 * (contains the name and score of students). Output a string according
 * to the format rules:
 *
 * 1. First line: Centering alignment. If spaces not even, right gets 1 more.
 * 2. Left part (names): Flush left. Width depends on longest string.
 * 3. Right part (scores): Both ends aligned. Width depends on longest string.
 *    Average score rounded to 1 decimal place.
 * 4. No sorting needed.
 */
function formatScoreList(list) {
}