/*
T.T.T.21: Handle the text alignment

You are a teacher and here is a list of test scores. Please output them in accordance with the rules.

Complete function formatScoreList() that accept an object array list (contains the name and score
of students). Output a string formatted as a bordered table.

Rules:
1. The first line (header) is centered. If the number of spaces is not even, the right side gets
   one more space than the left.
2. The left part (names list) is flush left. Its width depends on the longest name (and
   "The average score").
3. The right part (scores) is "both ends aligned": score on the left, "pts" on the right, padded
   between. The score column width depends on the longest score string. The average score is
   rounded to 1 decimal place.
4. No need to sort the list.
*/

function formatScoreList(list) {

}