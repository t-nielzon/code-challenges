/*
 * Prime number decompositions
 *
 * Implement three functions that take an integer n and return its prime
 * factorization in different formats.
 *
 * Function 1: getAllPrimeFactors(n)
 * Returns an array of prime factors in ascending order with repetition.
 * Example: n = 100 -> [2, 2, 5, 5]
 *
 * Function 2: getUniquePrimeFactorsWithCount(n)
 * Returns [[unique primes], [their powers]].
 * Example: n = 100 -> [[2, 5], [2, 2]]
 *
 * Function 3: getPrimeFactorPotpieces(n)
 * Returns array of each prime raised to its power.
 * Example: n = 100 -> [4, 25]
 *
 * Invalid input (not a number, not integer, negative, or 0):
 *   Function 1: [], Function 2: [[], []], Function 3: []
 *
 * Edge cases:
 *   n = 1 -> [1], [[1],[1]], [1]
 *   n = 2 -> [2], [[2],[1]], [2]
 */

function getAllPrimeFactors(n) {
}

function getUniquePrimeFactorsWithCount(n) {
}

function getPrimeFactorPotpieces(n) {
}