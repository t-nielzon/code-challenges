/*
A binary search tree is a binary tree that is ordered. This means that if you 
were to convert the tree to an array using an in-order traversal, the array 
would be in sorted order.

In this kata, you will write a function that will validate that a given binary 
tree is a binary search tree. The sort order is not predefined so it should 
work with either ascending or descending order.

A binary tree node is represented as:
  function Node(value, left, right) {
    this.value = value;
    this.left = left;
    this.right = right;
  }

@param {Node} node - The root node of a binary tree
@return {boolean} - True if the tree is a valid binary search tree, false otherwise
*/

function isBst(node) {
  // Your solution here
}