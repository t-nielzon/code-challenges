/*
 * Binary search tree validation
 *
 * A binary search tree is a binary tree that is ordered. This means that if
 * you were to convert the tree to an array using an in-order traversal, the
 * array would be in sorted order.
 *
 * Write a function that will validate that a given binary tree is a binary
 * search tree. The sort order is not predefined so it should work with either
 * ascending or descending order.
 *
 * Note: no test case tree will contain duplicate numbers.
 */

function isBST(node) {
  // your code here
}