/*
 * Binary search tree validation
 * Difficulty: 5 kyu
 *
 * A binary search tree is a binary tree that is ordered. This means that if you
 * were to convert the tree to an array using an in-order traversal, the array
 * would be in sorted order.
 *
 * Write a function that will validate that a given binary tree is a binary
 * search tree. The sort order is not predefined so it should work with either
 * ascending or descending order.
 *
 * Note: no test case tree will contain duplicate numbers.
 *
 * Each node has the shape: { value, left, right }
 * where left and right are either another node or null.
 */

function isBST(root) {
  // your code here
}