/*
 * Compute Unix path
 * 6 kyu
 *
 * Your task is simple: combine input parts into a Unix style path.
 *
 * The parameters are represented by strings which may contain letters,
 * dots, spaces, slashes and backslashes.
 *
 * Backslashes must be converted into slashes, spaces must be trimmed,
 * empty parts must be removed and trailing slashes should be removed.
 *
 * An empty path should be transformed into a slash.
 */

function compute(...parts) {

}

module.exports = { compute };